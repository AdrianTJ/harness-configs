{
  "skill_name": "docs-freshness",
  "evals": [
    {
      "id": "checks-docs-before-coding",
      "name": "verifies fast-moving API usage against current docs",
      "prompt": "Write a script that creates a GitHub issue with a label using `gh issue create`. I think the flag is `--label` but I'm not sure it takes multiple values in the installed version.",
      "expected_output": "Checks the installed gh version and the current docs for the flag before writing the script.",
      "assertions": [
        "The response checks the installed version and consults current official docs rather than quoting the flag from memory.",
        "The response states what version its answer targets when it matters."
      ]
    }
  ],
  "trigger_probes": [
    {
      "id": "fires-on-old-boto",
      "prompt": "Write a script that uploads a file to S3 using boto3. I recall upload_file takes a Callback but the version we run is old \u2014 just write it from what you know.",
      "fingerprints": [
        "primary source",
        "installed version"
      ]
    }
  ]
}
