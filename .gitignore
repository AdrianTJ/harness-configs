# Never let harness state or credentials into this repo. If a harness ever
# writes into a directory linked from here, these patterns catch it.
.credentials.json
auth.json
credentials.json
*.local.json
settings.local.json
session.json
sessions/
trust.json
history.jsonl
*.log

# Extension/plugin build output
node_modules/
dist/
*.tsbuildinfo

# Backups install.sh moves aside
*.bak-*

.DS_Store
